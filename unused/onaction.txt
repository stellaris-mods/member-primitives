
# This = construction ship
# From = planet it is built on
# Fires when construction is complete,
# immediately before station is created
#on_building_outpost_station = {
#	events = {
#		fp_remove.1
#	}
#}

# Triggers country_event for the attacker upon victory
# This = country, leader attacker
# From = country, planet owner
# FromFrom = planet
on_planet_attackers_win = {
	events = {
		# We have colonized the planet, if it was primitive.
		# This can happen before we have obspost, so we need to remove special project then
		fp_remove.2
	}
}
# A planet has begun the colonization process.
# Scope = Planet
on_colonization_started = {
	events = {
		# We have colonized the planet, if it was primitive.
		# This can happen before we have obspost, so we need to remove special project then
		# Probably enough with invasion? But ye lets be certain
		fp_remove.3
	}
}

#From = Country scope
#This = Planet scope
on_planet_transfer = {
	events = {
		fp_remove.4
	}
}
#From = Country scope
#This = Planet scope
on_planet_ownerless = {
	events = {
		fp_remove.4
	}
}

# Triggers when a station is abandoned.
# this = ship of station being lost.
# from = country losing ownership.
on_border_change_station_lost = {
	events = {
		fp_remove.5
	}
}

# Triggered when an Empire has fleets within another Empire's borders.
# This = receiver
# From = sender
# FromFrom = fleet
# FromFromFrom = system
on_enforce_borders = {
	events = {
		fp_remove.6
	}
}

# This = owner of fleet
# From = disbanded fleet
#on_fleet_disbanded = {
#	events = {
#		fp_remove.7
#	}
#}
# This = owner of fleet
# From = destroyed fleet
#on_fleet_destroyed_maintenance = {
#	events = {
#		fp_remove.8
#	}
#}
